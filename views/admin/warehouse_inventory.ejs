<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tồn kho - 2FATE Motor</title>

    <!-- Đồng bộ với admin dashboard -->
    <link rel="stylesheet" href="/css/admin.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">

    <link rel="stylesheet" href="/css/css_admin/warehouse_inventory.css">
</head>

<body>
    <%- include('../partials/header_admin') %>

        <div class="container-fluid">
            <div class="row">

                <%- include('../partials/sidebar_admin') %>

                    <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4 mt-4">

                        <!-- Title -->
                        <div class="d-flex justify-content-between align-items-center mb-4">
                            <h1 class="text-primary">
                                <i class="bi bi-building"></i>
                                <%= warehouse.name %>
                            </h1>
                            <a href="/admin/warehouses" class="btn btn-secondary shadow-sm">
                                <i class="bi bi-arrow-left"></i> Quay lại
                            </a>
                        </div>

                        <!-- Warehouse Info -->
                        <div class="report-card mb-4">
                            <h5 class="mb-2"><i class="bi bi-info-circle text-primary"></i> Thông tin kho</h5>
                            <p><strong>Địa chỉ:</strong>
                                <%= warehouse.address %>
                            </p>
                            <p><strong>Điện thoại:</strong>
                                <%= warehouse.phone || 'Chưa có' %>
                            </p>
                        </div>

                        <!-- Statistics -->
                        <div class="row mb-4">
                            <div class="col-md-3 mb-3">
                                <div class="stat-box blue">
                                    <div class="stat-value">
                                        <%= inventory.length %>
                                    </div>
                                    <div class="stat-label">Tổng SKU</div>
                                </div>
                            </div>

                            <div class="col-md-3 mb-3">
                                <div class="stat-box green">
                                    <div class="stat-value">
                                        <%= inventory.reduce((s, i)=> s + i.quantity, 0) %>
                                    </div>
                                    <div class="stat-label">Tổng số lượng</div>
                                </div>
                            </div>

                            <div class="col-md-3 mb-3">
                                <div class="stat-box orange">
                                    <div class="stat-value">
                                        <%= inventory.filter(i=> i.quantity <= i.min_quantity).length %>
                                    </div>
                                    <div class="stat-label">Sắp hết hàng</div>
                                </div>
                            </div>

                            <div class="col-md-3 mb-3">
                                <div class="stat-box red">
                                    <div class="stat-value">
                                        <% let totalValue=inventory.reduce((s, i)=> s + i.total_value, 0);
                                            let millions = (totalValue / 1000000).toFixed(1);
                                            %>
                                            <%= millions %>M
                                    </div>
                                    <div class="stat-label">Tổng giá trị (₫)</div>
                                </div>
                            </div>
                        </div>

                        <!-- Filters -->
                        <div class="report-card mb-4">
                            <div class="row g-3">
                                <div class="col-md-5">
                                    <input type="text" id="searchInput" class="form-control"
                                        placeholder="🔍 Tìm sản phẩm...">
                                </div>
                                <div class="col-md-4">
                                    <select class="form-select" id="statusFilter">
                                        <option value="">Tất cả trạng thái</option>
                                        <option value="low">Cần nhập thêm</option>
                                        <option value="normal">Bình thường</option>
                                        <option value="high">Tồn kho cao</option>
                                    </select>
                                </div>
                                <div class="col-md-3">
                                    <button class="btn btn-secondary w-100 shadow-sm" onclick="clearFilters()">
                                        <i class="bi bi-x-circle"></i> Xóa lọc
                                    </button>
                                </div>
                            </div>
                        </div>

                        <!-- Inventory Table -->
                        <div class="report-card">
                            <h5 class="mb-3"><i class="bi bi-list-ul text-primary"></i> Danh sách tồn kho</h5>

                            <div class="table-responsive">
                                <table class="table table-hover">
                                    <thead class="table-light">
                                        <tr>
                                            <th width="70">Ảnh</th>
                                            <th>Sản phẩm</th>
                                            <th width="130">Giá</th>
                                            <th width="110">Tồn kho</th>
                                            <th width="110">Tối thiểu</th>
                                            <th width="110">Tối đa</th>
                                            <th width="140">Giá trị</th>
                                            <th width="150">Trạng thái</th>
                                        </tr>
                                    </thead>

                                    <tbody id="inventoryTableBody">

                                        <% if (inventory.length===0) { %>
                                            <tr>
                                                <td colspan="8" class="text-center py-4">
                                                    <i class="bi bi-inbox" style="font-size: 3rem; color: #ccc;"></i>
                                                    <p class="mt-2">Kho chưa có sản phẩm nào</p>
                                                </td>
                                            </tr>
                                            <% } else { %>

                                                <% inventory.forEach(item=> {
                                                    let statusClass = "badge-normal";
                                                    let statusText = "Bình thường";

                                                    if (item.quantity <= item.min_quantity) { statusClass="badge-low" ;
                                                        statusText="Cần nhập thêm" ; } else if (item.quantity>=
                                                        item.max_quantity) {
                                                        statusClass = "badge-high";
                                                        statusText = "Tồn kho cao";
                                                        }
                                                        %>

                                                        <tr class="product-row"
                                                            data-name="<%= item.name.toLowerCase() %>"
                                                            data-status="<%= statusClass.replace('badge-', '') %>">

                                                            <td><img src="<%= item.image %>" class="product-img"></td>

                                                            <td><strong>
                                                                    <%= item.name %>
                                                                </strong></td>

                                                            <td>
                                                                <%= new Intl.NumberFormat('vi-VN').format(item.price) %>
                                                                    ₫
                                                            </td>

                                                            <td><strong style="font-size: 1.2rem; color:#0d6efd;">
                                                                    <%= item.quantity %>
                                                                </strong></td>

                                                            <td>
                                                                <%= item.min_quantity %>
                                                            </td>

                                                            <td>
                                                                <%= item.max_quantity %>
                                                            </td>

                                                            <td><strong style="color:#198754;">
                                                                    <%= new
                                                                        Intl.NumberFormat('vi-VN').format(item.total_value)
                                                                        %> ₫
                                                                </strong></td>

                                                            <td>
                                                                <span class="<%= statusClass %>">
                                                                    <%= statusText %>
                                                                </span>
                                                            </td>
                                                        </tr>

                                                        <% }) %>
                                                            <% } %>

                                    </tbody>
                                </table>
                            </div>
                        </div>

                    </main>
            </div>
        </div>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

        <script>
            function filterTable() {
                const search = document.getElementById("searchInput").value.toLowerCase();
                const status = document.getElementById("statusFilter").value;
                const rows = document.querySelectorAll(".product-row");

                rows.forEach(row => {
                    const name = row.dataset.name;
                    const st = row.dataset.status;

                    let show = true;
                    if (search && !name.includes(search)) show = false;
                    if (status && st !== status) show = false;

                    row.style.display = show ? "" : "none";
                });
            }

            document.getElementById("searchInput").addEventListener("input", filterTable);
            document.getElementById("statusFilter").addEventListener("change", filterTable);

            function clearFilters() {
                document.getElementById("searchInput").value = "";
                document.getElementById("statusFilter").value = "";
                filterTable();
            }
        </script>

</body>

</html>